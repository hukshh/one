# ─────────────────────────────────────────────────────
# MeetingMind — Root Environment (dev only reference)
# See server/.env.example and client/.env.example
# for the full variable reference for each service
# ─────────────────────────────────────────────────────

# Shared across both services in local dev
DATABASE_URL="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/meetingmind?retryWrites=true&w=majority"

# Run `npm run dev` from the root to start both services
